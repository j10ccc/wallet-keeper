import { addressMonthIndex, addressRecordIndex } from "./address";
import { fetchRecords, diff, getMergeData } from "./sync";

export default {
  addressMonthIndex,
  addressRecordIndex,
  // fetchRecordsByMonth,
  fetchRecords,
  diff,
  getMergeData
};
